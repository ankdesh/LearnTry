#include <iostream>
#include <fstream>
#include <vector>
#include <numeric>

struct Gate {
    int id;
    std::vector<int>        int sum = std::accumulate(C[i].begin(), C[i].end(), 0)Yes" : "No") << std::endl;
        outputFile.close();
    } else {
        std::cerr << "Error opening output file: " << outputFilename << std::endl;
        return 1;
    }

    return 0;
}
